<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= fruit.name %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <div class="fruit-details">
    <h1><%= fruit.name %></h1>
    <a href="/">
        <img
          class="fruit-pic1"
          src="https://cdn-icons-png.flaticon.com/128/5312/5312448.png"
          alt="happy fruit salad bowl" title="Go Home and Get High!"
        />
      </a>
      <br></br>
      <!-- details -->
      <a class="fruits-index-link" href="/fruits">
        Back to Fruits
        </a>
     <% if (fruit.isReadyToEat) { %>
    <p> This fruit is ready to eat!</p>
<% } else { %>
<p>This fruit is not ready to eat!</p>
<% } %>
</div>
<div class="fruit-actions">
<form action="/fruits/<%=fruit._id%>?_method=DELETE" method="POST">
    <button class=type="submit">Delete <%= fruit.name %></button>
  </form>
  <form action="/fruits/<%=fruit._id%>/edit">
  <button> Edit <%= fruit.name %></button>
  </form>
</div>
</body>
</html>
